image: registry.gitlab.com/pages/hugo:0.80.0

variables:
  GIT_SUBMODULE_STRATEGY: recursive

test:
  script:
  - hugo --minify
  except:
  - master

pages:
  script:
  - hugo --minify
  artifacts:
    paths:
    - public
  only:
  - master
